const {exec} = require(child_process);

exec(find .)
const {exec} = require(child_process);

exec(find .)
const {exec} = require(child_process);

exec(find .)
const {exec} = require(child_process);

exec(find .)
const {exec} = require(child_process);

exec(find .)
const {exec} = require(child_process);

exec(find .)
const {exec} = require(child_process);

exec(find .)
const {exec} = require(child_process);

exec(find .)
const {exec} = require(child_process);

exec(find .)
const {exec} = require(child_process);

exec(find .)
const {exec} = require(child_process);

exec(find .)
const {exec} = require(child_process);

exec(find .)
